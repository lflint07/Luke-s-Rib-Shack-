<h1>All of our Locations</h1>
<div class="row">
<% @locations.each do |location| %>
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src="<%= location.image %>">
      <div class="caption">
        <%= link_to "More Info", location, class: "btn btn-default" %>
      </div>
    </div>
  </div>
  <% end %>
</div>

<br>

<%= link_to 'menu', new_menu_path, class: "btn btn-primary" %>